# Frontend (React + TypeScript + Tailwind)

Setup

1. Install dependencies: `npm install` in `frontend/`.
2. Run dev server: `npm run dev` (default port `3000`).

The app expects backend at `http://localhost:4000` and recommender at `http://localhost:8001`.
